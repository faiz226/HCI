Connect OpenRouter for Buddy chat (API key in `.env`, not in this file):

Model: `@preset/kimi-2-6-free-only` — do not use `@preset/best-free-chatbot` (OpenRouter rejects presets with more than 3 models).

Optional: `OPENROUTER_FALLBACK_MODELS` for rate-limit failover. See `.env.example`.
